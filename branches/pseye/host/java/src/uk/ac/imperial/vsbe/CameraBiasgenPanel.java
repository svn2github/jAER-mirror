
package uk.ac.imperial.vsbe;

import javax.swing.JPanel;
import java.awt.GridBagConstraints;
import java.util.Observer;
import java.util.Observable;

/**
 * A panel for controlling a PSEyeModelChip and PSEyeCamera
 * This is added to the content panel of BiasgenFrame. 
 * It encapsulates the PSEyeSettingPanel, CameraFramePanel, and PSEyeModelPanel
 *
 * @author  mlk
 */
public class CameraBiasgenPanel extends JPanel implements Observer {
    public CameraBiasgen<?> biasgen;
    GridBagConstraints gbc = new GridBagConstraints();
    
    public CameraBiasgenPanel(CameraBiasgen<?> biasgen) {
        this.biasgen = biasgen;
        if(biasgen == null) 
            throw new RuntimeException("null biasgen while trying to construct BiasgenPanel");

        initComponents();
        gbc.gridx = 0;
        gbc.gridy = 0;
        gbc.fill = GridBagConstraints.BOTH;
        biasgen.getChip().addObserver(this);
        setHardware(biasgen.getChip().getHardwareInterface());
    }   
    
    private void setHardware(HardwareInterface hw) {
        if (hw != null && hw instanceof Camera) {
            Camera camera = (Camera) hw;
            gbc.gridx = 0;
            gbc.gridy = 2;
            this.add(camera.getControlPanel(), gbc);        
        }
        
    }
    
    @Override
    public void update(Observable o, Object arg) {
        if (o != null && o == biasgen.getChip() && arg instanceof Camera) {
            setHardware(arg);
            revalidate();
        }
    }
    /** This method is called from within the constructor to
     * initialize the form.
     * WARNING: Do NOT modify this code. The content of this method is
     * always regenerated by the Form Editor.
     */
    // <editor-fold defaultstate="collapsed" desc="Generated Code">//GEN-BEGIN:initComponents
    private void initComponents() {

        setLayout(new java.awt.GridBagLayout());
    }// </editor-fold>//GEN-END:initComponents
    // Variables declaration - do not modify//GEN-BEGIN:variables
    // End of variables declaration//GEN-END:variables
}
